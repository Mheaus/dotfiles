[color]
  branch = auto
  diff = auto
  interactive = auto
  status = auto
  ui = auto

[color "branch"]
  current = green
  remote = yellow

[core]
  pager = less -FRSX
	editor = code --wait

[alias]
  co = checkout
  st = status -sb
  br = branch
  ci = commit
  fo = fetch origin
  d = !git --no-pager diff
  dt  = difftool
  stat = !git --no-pager diff --stat

  # Clean merged branches
  sweep = !git branch --merged $(git symbolic-ref --short HEAD) | grep -v $(git symbolic-ref --short HEAD) | xargs git branch -d && git remote prune origin

  # http://www.jukie.net/bart/blog/pimping-out-git-log
  lg = log --graph --all --pretty=format:'%Cred%h%Creset - %s %Cgreen(%cr) %C(bold blue)%an%Creset %C(yellow)%d%Creset'

  m = checkout main

  # Removes a file from the index
  unstage = reset HEAD --

[help]
  autocorrect = 1

[push]
	autoSetupRemote = true

[fetch]
	prune = true

[rebase]
	autostash = true

[diff]
	algorithm = histogram

[branch "main"]
  mergeoptions = --no-edit
[user]
	name = Mathieu Audebert
	email = mathieu@sakuga.dev
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[credential "https://github.com"]
	helper =
	helper = !gh auth git-credential
[pull]
	rebase = true
[init]
	defaultBranch = main
